import React from 'react';
import { Typography } from '@material-ui/core';

export default function Projects() {
    return(
        <div id="projects" >
            <Typography variant="h2" align='left' style={{ flex:1, }} >My Work</Typography>
        </div>
    );
}